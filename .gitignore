# Артефакты сборки
build/

# Логи консоли сборок HMI (Ctrl+Alt+Z/X/C)
logs/

# Записанные макросы
macros/

# Сохранённые флаги хоткеев (например --test-ui)
**/Levorte.state.ini

# Бинарники инструментов (исходники .ahk в tools/ в репозитории)
tools/*.exe

# Windows / IDE
Thumbs.db
Desktop.ini
.cursor/
